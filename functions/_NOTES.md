# Functions — the site's AI server code — getridoftheteacher.com

Small Cloudflare Pages Functions that power the AI: find-game.js recommends games; ask-notes.js answers from the master notes. Runs on Workers AI (env.AI).

---

## 📝 Master notes — the shared brain

This folder is part of **getridoftheteacher.com**. The master notes for the whole site — the one shared source of truth that both the website AI and Claude read from — live in **`the-notes.md`** at the site root (viewable at **https://getridoftheteacher.com/the-notes.html**).

**Landed here and need to know how the site works? Go to the master notes.** Learned something new about this folder? Add it to the master notes so everyone stays in sync.
